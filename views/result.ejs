<%- include ('header') %>

  <h1>Result</h1>
    <p><%= result %></p>
    <p><%= resultObj["1"] %></p>
    <div style="width: 800px">
      <canvas id="myChart"></canvas>
    </div>
    <script>
      const ctx = document.getElementById('myChart');

      const resultObj = <%= JSON.stringify(resultObj) %>;
    
      new Chart(ctx, {
        type: 'bar',
        data: {
          labels: Object.keys(resultsObj),
          datasets: [{
            label: 'Your results',
            data: Object.values(resultObj),
            borderWidth: 1
          }]
        },
        options: {
          scales: {
            y: {
              beginAtZero: true
            }
          }
        }
      });
    </script>

<%- include ('footer') %>